import 'package:flutter/material.dart';

class HubRulesScreen extends StatelessWidget {
  const HubRulesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Rules: Device • Location • Time"),
    );
  }
}
